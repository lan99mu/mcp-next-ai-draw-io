---
name: drawio-diagramming
description: "mcp-next-ai-draw-io 仓库里的 .drawio 画图工作流与仓库规范。Use when: 在本仓库新增/修改架构图、通信图、领域模型图、流程图；需要批量加节点/连线、自动布局、减少线交叉、统一命名与分层；并输出可验收的变更摘要。"
argument-hint: "给我：目标 .drawio 路径（或新建目录）+ 图类型 + 节点清单 + 关系清单（表格/JSON 都可）+ 主题范围（这张图回答什么问题）"
user-invocable: true
---

# Draw.io 画图（mcp-next-ai-draw-io 仓库规范版）

目标：把架构/流程/领域关系**稳定**落到仓库的 `.drawio` 文件中，并确保：
- 图可读、可维护、可检索
- 修改可追踪、可验收、尽量避免冲突/覆盖
- 优先复用本项目已有的 MCP 工具能力，而不是手工改 XML

---

## 1. 仓库路径与命名规范（强约束）

### 1.1 允许的根目录
- 默认根目录：`系统架构图/`
- 如果需求里给出的目标路径不在 `系统架构图/` 下，应先确认是否允许例外；若未确认，优先建议迁移到 `系统架构图/`

### 1.2 文件命名
建议格式（二选一）：
1. `系统架构图/<域>/<主题>-<图类型>.drawio`
2. `系统架构图/<域>/<序号>-<主题>-<图类型>.drawio`

约定：
- `<图类型>`：`arch` / `comm` / `domain` / `flow`
- 文件名避免 `Untitled`
- 文件名尽量短、稳定、可检索；图内内容可以中文，文件名尽量避免纯中文长串

---

## 2. 最低输入契约（缺一项就追问）

### 2.1 必填
1. **目标文件**
   - 现有：`系统架构图/.../*.drawio`
   - 新建：给目录 + 文件名建议
2. **图类型**
   - 架构图 / 组件通信图 / 领域模型图 / 业务流程图
3. **主题范围（一句话）**
   - 例如：“广告投放链路中，投放服务与计费服务之间的调用/事件关系”
4. **节点清单**
5. **关系清单**
   - 至少 1 条关系

### 2.2 推荐（缺失可使用默认值）
- 方向：`LR`（左→右）或 `TB`（上→下），默认 `LR`
- 同步/异步标记规则，默认：
  - 调用 = 同步实线
  - 事件 = 异步虚线
- 是否需要外部系统分组，默认需要

### 2.3 推荐关系表格式

| from | to | 动作(动词) | 载体/协议 | 同步? | 备注 |
|---|---|---|---|---|---|
| A | B | 调用 | HTTP | 是 | /api/xxx |
| B | MQ | 发布 | Kafka | 否 | topic=xxx |

---

## 3. 统一绘图语义（风格规范）

### 3.1 形状语义
- `container`：域 / 子系统边界（分组）
- `rectangle`：服务 / 组件 / 模块
- `cylinder`：数据库 / 存储
- `cloud`：外部系统 / 第三方
- 流程图：`activity_start` / `activity_action` / `activity_decision` / `activity_end`
- 领域模型：`uml_class` / `uml_enum` / `uml_interface`

### 3.2 线条语义
- `调用(HTTP/gRPC)`：实线 + classic 箭头
- `发布/订阅(Kafka/RabbitMQ)`：虚线 + classic 箭头
- `写库/读库(MySQL/Redis)`：实线
- **所有边必须有 label**
  - 例外只在需求里显式声明
  - 如果标签需要多行，优先使用 `<br>`，与本项目现有 HTML label 约定保持一致

### 3.3 布局规则（优先级从高到低）
1. 减少线交叉
2. 同类节点对齐、留白一致
3. 方向一致（LR 或 TB 不混用）
4. 外部系统与内部系统必须视觉区隔

---

## 4. 本项目工具映射（必须优先使用）

本仓库已经提供完整的 Draw.io MCP 工具链，按下列方式使用：

### 4.1 文件与检查
- `load_diagram(path=...)`：加载已有图
- `create_diagram(name=...)`：新建图
- `save_diagram(path=...)`：保存图
- `list_cells()`：查看当前图全部元素与绑定关系
- `get_cell(cell_id=...)`：查看单个节点/连线的细节、几何信息和绑定关系

### 4.2 新增与修改
- `add_shape(...)`：新增节点/容器
- `add_connection(...)`：新增连线，必须填写 `label`
- `update_cell(...)`：改文案、坐标、样式、尺寸等
- `delete_cell(cell_id=...)`：删除错误元素

### 4.3 布局与局部调整
- `bind_nodes(node_ids=[...])`：把相关节点绑定成一个局部组
- `move_shape(shape_id=..., new_x=..., new_y=...)`：移动一个节点时带动整个绑定组
- `suggest_bindings()`：发现应该绑定但尚未绑定的邻近节点
- `detect_line_crossings()`：检测连线交叉，用于布局验收

### 4.4 工具使用原则
1. **先读后改**：先 `load_diagram` / `list_cells` / `get_cell` 再改
2. **成组即绑定**：新增一组相关节点后，尽快 `bind_nodes`
3. **优先局部移动**：要调布局时优先移动绑定组中的一个节点，而不是分别改多个节点
4. **每次改动后验收**：用 `detect_line_crossings` + `list_cells` 复核结果

---

## 5. 标准改图流程（执行顺序）

### 5.1 修改前安全策略
- 读取现有图并定位目标区域，优先按 label 检索
- 如果用户要求“保守修改”，先另存备份文件，例如：`xxx_20260314.drawio`
- 如果是新图，先确认目录和文件名，再 `create_diagram`

### 5.2 标准步骤
1. **加载或创建**
   - 已有图：`load_diagram`
   - 新图：`create_diagram`
2. **扫描现状**
   - `list_cells`
   - 必要时对关键节点/连线调用 `get_cell`
3. **新增或调整节点**
   - 使用约定形状语义
   - 同类节点尺寸尽量统一
4. **新增或调整连线**
   - 所有连线都带动作 label
   - 协议、事件名、topic、接口路径等在 label 或备注中体现
5. **绑定局部组**
   - 服务 + DB、服务 + 缓存、容器内的同层节点等，优先绑定
   - 用 `suggest_bindings()` 补查遗漏
6. **布局优化**
   - 先看 `detect_line_crossings()`
   - 再用 `move_shape()` 做局部调整，尽量把交叉降到 0
7. **保存**
   - `save_diagram(path=...)`

---

## 6. 分图类型的专用规则

### 6.1 架构图（arch）
- 先画系统边界，再放服务、存储、外部依赖
- 推荐分层：入口层 / 应用层 / 数据层 / 外部系统
- 同层组件优先对齐

### 6.2 组件通信图（comm）
- 重点表达“谁调谁 / 谁发给谁 / 经过什么协议”
- 同步调用与异步事件必须视觉上可区分
- 一条边只表达一个主要动作，避免在一条边上混写多种语义

### 6.3 领域模型图（domain）
- 优先使用 `uml_class` / `uml_enum` / `uml_interface`
- 类名、属性、方法、关系类型要统一命名
- 需要多行文本时使用 HTML `<br>` label

### 6.4 业务流程图（flow）
- 起止节点清晰
- 决策节点必须有分支条件
- 流程主路径优先保持单方向阅读

---

## 7. 命名与文案规范

- 同一概念在同一张图中只能有一个稳定叫法
- 节点名优先使用业务上稳定的名词，不用临时口语
- 连线 label 必须是“动词/动作短语”，例如：
  - `调用下单`
  - `发布订单已支付`
  - `读取用户画像`
- 如果同一节点既有中文名又有英文系统名，建议写成：`投放服务 AdService`

---

## 8. 质量门禁（每次修改必查）

保存前至少检查：
1. **线交叉数**
   - `detect_line_crossings()` 的结果
2. **无 label 的边数量**
   - 通过 `list_cells()` / `get_cell()` 自查
3. **未分组的外部系统**
   - 是否已经通过容器、虚线边框或 `cloud` 形状进行视觉区隔
4. **命名一致性**
   - 同一概念是否出现多种叫法
5. **局部可维护性**
   - 可以一起移动的节点是否已绑定

---

## 9. 每次交付的固定输出

每次完成改图后，必须同时输出：

1. **保存的 `.drawio` 路径**
2. **变更摘要**
   - 新增节点：`[...]`
   - 新增连线：`[...]`
   - 删除/替换节点：`[...]`
   - 调整布局：`[...]`
3. **质量检查结果**
   - 线交叉：`X`
   - 无 label 的边：`Y`
   - 未分组的外部系统：`是 / 否`

---

## 10. 启动对话时最省时间的输入模板

直接提供以下内容即可开始：

- 目标路径（或新建目录 + 文件名）
- 图类型
- 主题范围一句话
- 节点清单（名字 + 是否外部即可）
- 关系表（推荐用表格）

示例：

```yaml
target: 系统架构图/广告/ad-delivery-comm.drawio
diagram_type: comm
scope: 广告投放链路中投放服务与计费服务之间的调用和事件关系
direction: LR
nodes:
  - name: 投放服务
    external: false
  - name: 计费服务
    external: false
  - name: Kafka
    external: false
  - name: 广告主后台
    external: true
relations:
  - from: 广告主后台
    to: 投放服务
    action: 调用创建投放
    protocol: HTTP
    sync: true
  - from: 投放服务
    to: Kafka
    action: 发布投放已创建
    protocol: Kafka
    sync: false
  - from: 计费服务
    to: 投放服务
    action: 调用查询投放
    protocol: gRPC
    sync: true
```

拿到这些信息后，应按本技能文档直接执行改图、保存并给出验收摘要。
